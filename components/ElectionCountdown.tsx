"use client";import {useEffect,useState} from "react";const target=new Date("2026-10-04T08:00:00-03:00").getTime();export default function ElectionCountdown(){const [d,setD]=useState({days:0,hours:0,minutes:0});useEffect(()=>{const calc=()=>{const x=Math.max(0,target-Date.now());setD({days:Math.floor(x/86400000),hours:Math.floor(x/3600000)%24,minutes:Math.floor(x/60000)%60})};calc();const i=setInterval(calc,60000);return()=>clearInterval(i)},[]);return <div className="card p-6 bg-gradient-to-br from-[#35185e] to-[#6740a0] text-white"><div className="badge !text-white !border-white/20 !bg-white/10">Eleições 2026</div><h2 className="text-2xl font-bold mt-3">Primeiro turno: 4 de outubro</h2><p className="text-white/70">Segundo turno: 25 de outubro, onde houver</p><div className="grid grid-cols-3 gap-3 mt-6">{[[d.days,"dias"],[d.hours,"horas"],[d.minutes,"min"]].map(([v,l])=><div key={l} className="bg-white/10 rounded-2xl p-4 text-center"><div className="text-3xl font-black">{v}</div><div className="text-xs text-white/70">{l}</div></div>)}</div><div className="mt-5 text-sm">Fase estratégica demonstrativa: Organização e preparação</div></div>}