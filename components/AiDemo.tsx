"use client";
import {useState} from "react";import Shell from "./Shell";import {Bot,Send,Sparkles,User} from "lucide-react";import {strategies} from "@/data/strategies";
const suggestions=["Crie um plano de humanização","Como responder a uma crítica?","Monte uma estratégia para podcast","Quais cuidados usar com humor?"];
function answer(q:string){const t=q.toLowerCase();const s=t.includes("humor")?strategies[4]:t.includes("crítica")||t.includes("responder")?strategies[5]:t.includes("podcast")?strategies[3]:t.includes("human")?strategies[2]:strategies[0];return `DIAGNÓSTICO\n${s.objective}\n\nEVIDÊNCIA DISPONÍVEL\n${s.rationale}\n\nRECOMENDAÇÕES\n• ${s.actions.slice(0,3).join("\n• ")}\n\nLIMITAÇÕES\nResposta demonstrativa baseada na síntese local dos estudos. Não utiliza dados reais dos perfis nem garante resultado.`}
export default function AiDemo(){const [messages,setMessages]=useState<{role:"user"|"bot",text:string}[]>([{role:"bot",text:"Olá. Sou o Arcanum Assistente em modo demonstração. Posso organizar as estratégias locais, mas não estou conectado a uma API nem a dados reais."}]);const [input,setInput]=useState("");function send(text=input){if(!text.trim())return;setMessages(m=>[...m,{role:"user",text},{role:"bot",text:answer(text)}]);setInput("")}return <Shell title="Arcanum Assistente"><div className="ai-shell"><div className="ai-header"><div><span className="eyebrow">MODO DEMONSTRAÇÃO</span><h1>Conhecimento estratégico organizado</h1><p>Respostas locais baseadas nas sínteses incorporadas ao projeto.</p></div><Sparkles/></div><div className="suggestion-row">{suggestions.map(s=><button onClick={()=>send(s)} key={s}>{s}</button>)}</div><div className="chat-stream">{messages.map((m,i)=><div className={`chat-message ${m.role}`} key={i}><div className="chat-avatar">{m.role==="bot"?<Bot size={18}/>:<User size={18}/>}</div><div><span>{m.role==="bot"?"Arcanum":"Você"}</span><p>{m.text}</p></div></div>)}</div><div className="chat-input"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Pergunte sobre estratégia, conteúdo ou reputação..."/><button onClick={()=>send()}><Send size={18}/></button></div></div></Shell>}
